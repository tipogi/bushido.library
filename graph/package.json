{
  "name": "graph",
  "version": "0.1.0",
  "description": "Creates the nodes need it for Neo4J creating json file",
  "scripts": {
    "start:build": "tsc -w",
    "start:run": "nodemon dist/index.js",
    "start": "concurrently npm:start:*"
  },
  "author": "tipogi",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.2.0"
  }
}
